services:
  benchmark:
    build: .
    volumes:
      - ./results:/results
    environment:
      # all repos
      #      - REPO_URLS=https://github.com/JavaScript-package-manager-comparison/art-design-pro.git https://github.com/JavaScript-package-manager-comparison/metacubexd.git https://github.com/JavaScript-package-manager-comparison/superset.git
      #
      # small repo only
      #      - REPO_URLS=https://github.com/JavaScript-package-manager-comparison/art-design-pro.git
      #
      # medium repo only
      #      - REPO_URLS=https://github.com/JavaScript-package-manager-comparison/metacubexd.git
      #
      # big repo only
      #      - REPO_URLS=https://github.com/JavaScript-package-manager-comparison/superset.git
      # medium and big
      - REPO_URLS=https://github.com/JavaScript-package-manager-comparison/metacubexd.git https://github.com/JavaScript-package-manager-comparison/superset.git
      - RUNS=10
      - WARMUP=1