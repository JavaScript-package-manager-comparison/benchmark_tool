services:
  benchmark:
    build: .
    volumes:
      - ./results:/results
    environment:
      - REPO_URL=https://github.com/JavaScript-package-manager-comparison/metacubexd.git
      - RUNS=5
      - WARMUP=1