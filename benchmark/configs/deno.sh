declare -A deno_config
deno_config[version]="deno@2.8.2"
deno_config[full_install]="deno install --node-modules-dir=auto"
deno_config[ci_install]="deno install --node-modules-dir=auto --frozen"
deno_config[offline_install]="DENO_NO_UPDATE_CHECK=1 deno install --node-modules-dir=auto"
deno_config[offline_ci_install]="DENO_NO_UPDATE_CHECK=1 deno install --node-modules-dir=auto --frozen"
deno_config[clean_cache_cmd]="rm -rf \${DENO_DIR:-\$HOME/.cache/deno}/npm \${DENO_DIR:-\$HOME/.cache/deno}/dep 2>/dev/null || true"
deno_config[disk_dir]="node_modules"
deno_config[lockfile]="deno.lock"
deno_config[note]="Deno (${deno_config[version]})"
deno_config[activate_cmd]="curl -fsSL https://deno.land/install.sh | sh && export DENO_INSTALL=\$HOME/.deno && export PATH=\$DENO_INSTALL/bin:\$PATH && deno --version"
